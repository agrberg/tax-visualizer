# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Raw 1040 returns dropped in to build fixtures — real PII, never committed (see
# src/import/fixtures/README.md). Only the anonymized rebuilds under src/import/fixtures/ are checked in.
fixtures-src

# Editor directories and files
.claude
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
